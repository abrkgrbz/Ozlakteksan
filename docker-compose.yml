version: '3.8'

services:
  ozlakteksan-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ozlakteksan-web
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
    restart: unless-stopped
    networks:
      - ozlakteksan-network
    volumes:
      - ./logs:/app/logs

networks:
  ozlakteksan-network:
    driver: bridge

volumes:
  logs:
